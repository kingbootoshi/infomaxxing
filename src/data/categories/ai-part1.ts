import { Concept } from '@/lib/types';

export const aiPart1: Concept[] = [
  {
    id: "neural-network",
    term: "Neural Network",
    category: "ai",
    tags: ["ml", "models", "fundamentals"],
    oneLiner: "A neural network learns by stacking simple math functions into a system that can approximate complex patterns.",
    body: "A neural network is made of layers of units that compute weighted sums of inputs and pass the result through nonlinear functions. During training, the model compares its predictions to the correct answers and adjusts those weights to reduce error. The power comes from composition: many small transformations combine into a flexible function that can model images, text, audio, and structured data.",
    whyItMatters: "Neural networks are the core building block behind most modern AI systems. If you understand how they turn inputs into learned representations, the rest of deep learning concepts become much easier to reason about.",
    relatedTerms: ["Deep Learning", "Perceptron"],
  },
  {
    id: "deep-learning",
    term: "Deep Learning",
    category: "ai",
    tags: ["ml", "neural-networks", "training"],
    oneLiner: "Deep learning is neural network training at enough scale and depth to learn useful features automatically.",
    body: "Deep learning uses neural networks with many layers, which lets the model build up representations step by step. Early layers may capture simple signals like edges or local patterns, while deeper layers combine them into more abstract structure. Instead of hand-engineering features, engineers provide data, an objective, and a training setup that lets the network discover those features itself.",
    whyItMatters: "Much of modern AI progress came from deep learning replacing brittle, hand-built feature pipelines. It is the foundation for state-of-the-art systems in language, vision, speech, and generative models.",
    relatedTerms: ["Neural Network", "Transformer"],
  },
  {
    id: "perceptron",
    term: "Perceptron",
    category: "ai",
    tags: ["ml", "history", "fundamentals"],
    oneLiner: "The perceptron is the simplest neuron-like model: weighted inputs, one thresholded decision.",
    body: "A perceptron takes input values, multiplies them by learned weights, adds a bias term, and then applies a decision rule to produce an output. In its original form, it can only separate data that is linearly separable, which is a major limitation. It matters historically because modern neural networks are built by stacking and generalizing this basic idea.",
    whyItMatters: "The perceptron shows the basic mechanics of learned weights and bias without the complexity of modern architectures. It is the cleanest starting point for understanding why depth and nonlinearity became necessary.",
    relatedTerms: ["Neural Network", "Activation Function"],
  },
  {
    id: "activation-function",
    term: "Activation Function",
    category: "ai",
    tags: ["ml", "layers", "math"],
    oneLiner: "An activation function adds nonlinearity so a network can learn more than straight-line rules.",
    body: "Without an activation function, a stack of linear layers collapses into one linear transformation no matter how many layers you add. The activation changes each unit's output in a nonlinear way, which lets the network represent more complex decision boundaries and interactions. Different activations also affect gradient flow, numerical stability, and how efficiently a model trains.",
    whyItMatters: "Activation choice changes both what a model can represent and how hard it is to optimize. Many training successes or failures trace back to this small but critical design choice.",
    relatedTerms: ["ReLU", "Sigmoid"],
  },
  {
    id: "relu",
    term: "ReLU",
    category: "ai",
    tags: ["ml", "activation", "optimization"],
    oneLiner: "ReLU keeps positive values and zeroes out negative ones.",
    body: "ReLU stands for Rectified Linear Unit and is defined as max(0, x). It is cheap to compute and avoids the heavy saturation problems of older activations over much of its range, which helped deep networks train more reliably. Its main drawback is that units can get stuck outputting zero if training drives them too far into the negative region.",
    whyItMatters: "ReLU became a default activation in many deep learning systems because it made large models easier to optimize. It is a good example of how a simple mathematical choice can change practical training behavior.",
    relatedTerms: ["Activation Function", "GELU"],
  },
  {
    id: "sigmoid",
    term: "Sigmoid",
    category: "ai",
    tags: ["ml", "activation", "probability"],
    oneLiner: "Sigmoid squashes any number into a smooth output between 0 and 1.",
    body: "The sigmoid function maps large negative values close to 0 and large positive values close to 1. That makes it useful when you want an output that behaves like a probability, especially in binary classification heads. In deeper hidden layers, though, sigmoid often causes vanishing gradients because its slope becomes very small in the saturated regions.",
    whyItMatters: "Sigmoid still matters because it shows the tradeoff between intuitive outputs and difficult optimization. Engineers see it often at model outputs even if they avoid it inside deep hidden stacks.",
    relatedTerms: ["Activation Function", "Softmax"],
  },
  {
    id: "softmax",
    term: "Softmax",
    category: "ai",
    tags: ["ml", "classification", "probability"],
    oneLiner: "Softmax turns a list of raw scores into a probability distribution over choices.",
    body: "Softmax exponentiates each score and normalizes by the total, so every output is positive and the values sum to 1. That makes it natural for multi-class prediction, where the model must allocate probability mass across many mutually exclusive options. Because exponentials amplify score differences, softmax can make a model look very confident even when its raw logits are only modestly separated.",
    whyItMatters: "Softmax is the standard bridge between model scores and class probabilities. It also appears inside attention, where it converts compatibility scores into normalized weights.",
    relatedTerms: ["Sigmoid", "Attention Mechanism"],
  },
  {
    id: "gelu",
    term: "GELU",
    category: "ai",
    tags: ["ml", "activation", "transformers"],
    oneLiner: "GELU gates values smoothly instead of cutting them off with a hard threshold.",
    body: "GELU stands for Gaussian Error Linear Unit and scales inputs by a smooth curve rather than simply zeroing negatives. Small negative values are only partially suppressed, which gives the model a softer transition than ReLU. Transformer feedforward blocks commonly use GELU because it performs well in large language models and preserves richer gradient behavior.",
    whyItMatters: "GELU is part of the standard recipe in many transformer architectures. It shows how activation design shifts when models become deeper, wider, and more sensitive to optimization details.",
    relatedTerms: ["ReLU", "Feedforward Network"],
  },
  {
    id: "feedforward-network",
    term: "Feedforward Network",
    category: "ai",
    tags: ["ml", "layers", "architecture"],
    oneLiner: "A feedforward network pushes information from input to output with no recurrence or state loop.",
    body: "In a feedforward network, each layer reads the output of the previous layer and sends its result forward. There is no memory of earlier timesteps and no feedback connection inside the basic structure. In transformers, the position-wise feedforward block expands each token representation, applies a nonlinearity, and projects it back down, giving the model extra capacity beyond attention alone.",
    whyItMatters: "Feedforward layers are where models do much of their local computation after information has been mixed. They are a core part of both classic multilayer perceptrons and modern transformer blocks.",
    relatedTerms: ["Neural Network", "Transformer"],
  },
  {
    id: "cnn-convolutional-neural-network",
    term: "CNN (Convolutional Neural Network)",
    category: "ai",
    tags: ["vision", "ml", "architecture"],
    oneLiner: "A CNN learns spatial patterns by sliding shared filters across the input.",
    body: "Convolution layers apply small kernels over local regions, which lets the model detect features like edges, textures, and shapes regardless of where they appear. Weight sharing makes CNNs much more parameter-efficient than fully connected layers on image-sized inputs. Pooling and deeper stacks then combine local features into higher-level visual structure.",
    whyItMatters: "CNNs drove major advances in computer vision and remain useful when locality and translation invariance matter. They are also a strong example of architecture choices reflecting the structure of the data.",
    relatedTerms: ["Neural Network", "Deep Learning"],
  },
  {
    id: "rnn-recurrent-neural-network",
    term: "RNN (Recurrent Neural Network)",
    category: "ai",
    tags: ["sequence-modeling", "ml", "architecture"],
    oneLiner: "An RNN processes sequences by carrying a hidden state forward one step at a time.",
    body: "At each timestep, an RNN combines the current input with a state vector that summarizes what it has seen so far. That shared recurrence lets it model ordered data such as language, audio, and time series without treating each position independently. Classic RNNs struggle with long-range dependencies because gradients shrink or explode as they move through many sequential steps.",
    whyItMatters: "RNNs were the main sequence modeling tool before transformers took over. Understanding their limits explains why architectures with better long-context behavior became so important.",
    relatedTerms: ["LSTM", "GRU"],
  },
  {
    id: "lstm",
    term: "LSTM",
    category: "ai",
    tags: ["sequence-modeling", "ml", "memory"],
    oneLiner: "LSTM adds gates and an explicit cell state so an RNN can retain information longer.",
    body: "LSTM stands for Long Short-Term Memory and introduces input, forget, and output gates that control what information is written, kept, and exposed. Its cell state gives gradients a cleaner path through time, which reduces the worst long-range training problems of vanilla RNNs. The model can still process sequences step by step, but it has a more durable internal memory.",
    whyItMatters: "LSTMs made sequence models practical for many real tasks before transformers became dominant. They remain a useful reference point for reasoning about memory, recurrence, and gating.",
    relatedTerms: ["RNN (Recurrent Neural Network)", "GRU"],
  },
  {
    id: "gru",
    term: "GRU",
    category: "ai",
    tags: ["sequence-modeling", "ml", "memory"],
    oneLiner: "GRU is a leaner gated RNN that keeps much of LSTM's benefit with fewer moving parts.",
    body: "GRU stands for Gated Recurrent Unit and uses update and reset gates instead of LSTM's larger gating scheme. It merges some of the state-handling logic into a simpler structure, which reduces parameter count and implementation complexity. In practice, GRUs often perform similarly to LSTMs on many sequence tasks while training a bit more efficiently.",
    whyItMatters: "GRUs show that better sequence memory did not require sticking with the original RNN design. They are a useful middle ground when comparing older recurrent architectures.",
    relatedTerms: ["RNN (Recurrent Neural Network)", "LSTM"],
  },
  {
    id: "residual-connection",
    term: "Residual Connection",
    category: "ai",
    tags: ["ml", "optimization", "architecture"],
    oneLiner: "A residual connection lets a layer learn a change to the input instead of rebuilding the whole signal from scratch.",
    body: "A residual connection adds a layer's input directly to its output, creating a shortcut path through the network. That makes very deep models easier to optimize because information and gradients can travel around poorly behaving layers instead of being forced through every transformation. Transformer blocks rely on residual paths around both attention and feedforward sublayers.",
    whyItMatters: "Residual connections are one of the reasons deep models became trainable at large scale. They reduce optimization friction and help preserve useful representations across many layers.",
    relatedTerms: ["Batch Normalization", "Transformer"],
  },
  {
    id: "batch-normalization",
    term: "Batch Normalization",
    category: "ai",
    tags: ["ml", "training", "stability"],
    oneLiner: "Batch normalization rescales activations using batch statistics to stabilize training.",
    body: "During training, batch normalization computes the mean and variance of activations over a mini-batch, normalizes them, and then applies learned scale and shift parameters. This reduces sensitivity to poor initialization and can make optimization faster by keeping activations in a more controlled range. It works well in many convolutional models, but its dependence on batch statistics makes it awkward for variable-length sequences and very small batch sizes.",
    whyItMatters: "Batch normalization was a major practical breakthrough for training deep networks reliably. It also highlights that the best normalization strategy depends heavily on the model architecture and training setup.",
    relatedTerms: ["Layer Normalization", "Residual Connection"],
  },
  {
    id: "layer-normalization",
    term: "Layer Normalization",
    category: "ai",
    tags: ["ml", "training", "transformers"],
    oneLiner: "Layer normalization normalizes within each example instead of across the batch.",
    body: "Rather than using statistics from other samples in the batch, layer normalization computes mean and variance across features for a single token or example. That makes it stable even when batch sizes are small or sequence lengths vary. Transformers use layer norm heavily because it fits token-wise computation and avoids the batch coupling that complicates sequence models.",
    whyItMatters: "Layer normalization is part of the standard transformer recipe. It is one reason large language models can train consistently across very different hardware and batching setups.",
    relatedTerms: ["Batch Normalization", "Transformer"],
  },
  {
    id: "dropout",
    term: "Dropout",
    category: "ai",
    tags: ["ml", "regularization", "training"],
    oneLiner: "Dropout randomly blanks part of the network during training so the model cannot rely too heavily on any one path.",
    body: "At training time, dropout sets a random subset of activations to zero and scales the remaining ones to keep expected magnitude roughly consistent. This forces the model to distribute useful behavior across many units instead of memorizing narrow co-adaptations. At inference time, dropout is turned off so the full network is used deterministically.",
    whyItMatters: "Dropout is a classic regularization trick for reducing overfitting. Even when newer large-scale recipes use it less aggressively, the idea remains central to how engineers think about model robustness.",
    relatedTerms: ["Overfitting", "Deep Learning"],
  },
  {
    id: "overfitting",
    term: "Overfitting",
    category: "ai",
    tags: ["ml", "generalization", "training"],
    oneLiner: "Overfitting means the model learned the training data too specifically and fails to generalize.",
    body: "An overfit model captures noise, quirks, or accidental correlations in the training set instead of the broader pattern you actually care about. It often looks impressive on training metrics while degrading sharply on validation or real-world data. The usual fixes involve more data, better regularization, smaller capacity, or training procedures that stop before memorization dominates.",
    whyItMatters: "A model that only performs well on the data it already saw is not useful in production. Generalization is the real target, and overfitting is the constant pressure pushing against it.",
    relatedTerms: ["Dropout", "Perplexity"],
  },
  {
    id: "transformer",
    term: "Transformer",
    category: "ai",
    tags: ["llms", "architecture", "attention"],
    oneLiner: "The transformer is a sequence model built around attention instead of recurrence.",
    body: "A transformer represents each token as a vector, mixes information across positions with attention, and then refines those representations with feedforward layers. Because tokens can attend to many other tokens directly, the model captures long-range dependencies without stepping through the sequence one item at a time like an RNN. This parallel structure is a major reason transformers scaled so well for language and multimodal learning.",
    whyItMatters: "Transformers are the backbone of modern language models and many generative systems. Most current AI terminology around attention, context windows, and scaling assumes this architecture.",
    relatedTerms: ["Attention Mechanism", "LLM (Large Language Model)"],
  },
  {
    id: "attention-mechanism",
    term: "Attention Mechanism",
    category: "ai",
    tags: ["llms", "architecture", "sequence-modeling"],
    oneLiner: "Attention lets a model decide which parts of the input matter most for the current computation.",
    body: "Instead of compressing everything into one fixed-size state, attention computes relevance scores between a query and a set of candidate items. After normalizing those scores, the model takes a weighted combination of the values, so more relevant inputs contribute more to the result. This gives the model dynamic access to different pieces of context depending on what it is trying to predict.",
    whyItMatters: "Attention is the mechanism that made long-range context handling much more effective in sequence models. It is also the conceptual bridge between classic sequence-to-sequence systems and today's transformers.",
    relatedTerms: ["Self-Attention", "Cross-Attention"],
  },
  {
    id: "self-attention",
    term: "Self-Attention",
    category: "ai",
    tags: ["llms", "attention", "transformers"],
    oneLiner: "Self-attention lets each token look at other tokens in the same sequence when updating its representation.",
    body: "For every position, the model builds query, key, and value vectors from the same input sequence. It compares the query at one position against keys at other positions to decide how much information to pull from their values. That makes each token representation context-aware rather than just a standalone embedding of the local word or symbol.",
    whyItMatters: "Self-attention is the core operation inside transformer language models. It is how the model connects distant words, code tokens, or symbols without hand-built rules about sequence structure.",
    relatedTerms: ["Attention Mechanism", "Multi-Head Attention"],
  },
  {
    id: "multi-head-attention",
    term: "Multi-Head Attention",
    category: "ai",
    tags: ["llms", "attention", "transformers"],
    oneLiner: "Multi-head attention runs several attention patterns in parallel and combines them.",
    body: "Instead of computing one attention map over the full representation, the model splits the representation into multiple subspaces called heads. Each head learns its own query, key, and value projections, so different heads can focus on different relationships such as syntax, locality, or long-range references. The outputs are concatenated and projected back into the model dimension.",
    whyItMatters: "Multiple heads give transformers a richer way to model structure than a single attention pattern would. This is one reason the architecture can represent many different relationships at once.",
    relatedTerms: ["Self-Attention", "Transformer"],
  },
  {
    id: "cross-attention",
    term: "Cross-Attention",
    category: "ai",
    tags: ["llms", "attention", "seq2seq"],
    oneLiner: "Cross-attention lets one sequence attend to a different sequence.",
    body: "In cross-attention, the query vectors come from one stream while keys and values come from another. A decoder can use this to look at encoder outputs, or a text model can use it to pull information from an image, retrieved document, or other modality. This is how models condition generation on external context instead of only their own prior tokens.",
    whyItMatters: "Cross-attention is central to encoder-decoder models, retrieval-augmented systems, and multimodal architectures. It is the mechanism that turns separate sources of information into one coordinated prediction process.",
    relatedTerms: ["Attention Mechanism", "Encoder-Decoder Architecture"],
  },
  {
    id: "flash-attention",
    term: "Flash Attention",
    category: "ai",
    tags: ["llms", "performance", "attention"],
    oneLiner: "Flash Attention makes attention faster and more memory-efficient by changing how the computation is scheduled.",
    body: "Standard attention often materializes large intermediate matrices in high-bandwidth memory, which becomes expensive for long sequences. Flash Attention reorganizes the computation in tiles so it can fuse steps, reduce memory traffic, and avoid storing the full attention matrix explicitly. The result is mathematically equivalent attention with much better hardware efficiency on modern accelerators.",
    whyItMatters: "Attention cost is one of the main bottlenecks in large models. Flash Attention matters because it turns the same model architecture into something much more practical to train and serve.",
    relatedTerms: ["Self-Attention", "Paged Attention"],
  },
  {
    id: "paged-attention",
    term: "Paged Attention",
    category: "ai",
    tags: ["llms", "inference", "performance"],
    oneLiner: "Paged Attention manages attention memory in blocks so serving long or many requests wastes less space.",
    body: "During LLM inference, the model stores key and value caches for every generated token so future tokens can attend back to them. Paged Attention organizes that cache in fixed-size pages rather than one large contiguous allocation per request, which reduces fragmentation and makes batching variable-length requests much more efficient. It is a serving optimization, not a new attention formula for model training.",
    whyItMatters: "Serving systems live or die on memory efficiency. Paged Attention is a major reason modern inference engines can keep more requests in flight on the same hardware.",
    relatedTerms: ["Flash Attention", "Context Window"],
  },
  {
    id: "positional-encoding",
    term: "Positional Encoding",
    category: "ai",
    tags: ["llms", "transformers", "sequence-modeling"],
    oneLiner: "Positional encoding gives a transformer some notion of order, since attention alone is permutation-invariant.",
    body: "If you feed a transformer token embeddings with no position signal, it can see which tokens exist but not where they occur. Positional encoding injects position information into the token representations so the model can distinguish 'dog bites man' from 'man bites dog.' Different schemes encode absolute or relative position in different ways, but they all solve the same basic problem: order is not free in attention models.",
    whyItMatters: "Language, code, and time-series data depend heavily on sequence order. Positional encoding is what keeps transformer models from treating a sentence like an unordered bag of tokens.",
    relatedTerms: ["RoPE (Rotary Positional Embeddings)", "Transformer"],
  },
  {
    id: "rope-rotary-positional-embeddings",
    term: "RoPE (Rotary Positional Embeddings)",
    category: "ai",
    tags: ["llms", "transformers", "sequence-modeling"],
    oneLiner: "RoPE encodes position by rotating query and key vectors in a way that preserves relative position information.",
    body: "Instead of adding a separate position vector to token embeddings, RoPE applies a position-dependent rotation inside attention computations. This makes attention scores depend naturally on relative offsets between tokens, which tends to work well for long-context language modeling. Many modern LLMs use RoPE because it integrates cleanly into transformer attention and extrapolates better than some older schemes.",
    whyItMatters: "RoPE is part of the standard design of many current language models. Engineers run into it quickly when thinking about long context behavior, fine-tuning, or context extension tricks.",
    relatedTerms: ["Positional Encoding", "Self-Attention"],
  },
  {
    id: "encoder-decoder-architecture",
    term: "Encoder-Decoder Architecture",
    category: "ai",
    tags: ["seq2seq", "transformers", "architecture"],
    oneLiner: "An encoder-decoder model reads one sequence into representations and then generates another sequence from them.",
    body: "The encoder processes the input sequence into contextual representations, while the decoder generates the output token by token. The decoder usually uses masked self-attention over what it has already generated and cross-attention over the encoder outputs. This pattern fits tasks where input and output play different roles, such as translation, summarization, and many multimodal systems.",
    whyItMatters: "Not every language model is a plain decoder-only GPT-style system. Encoder-decoder designs remain important when conditioning on a source sequence is central to the task.",
    relatedTerms: ["Cross-Attention", "Transformer"],
  },
  {
    id: "embedding",
    term: "Embedding",
    category: "ai",
    tags: ["representation-learning", "llms", "vectors"],
    oneLiner: "An embedding is a dense vector that puts discrete items into a space where similar meaning tends to land nearby.",
    body: "Neural models cannot operate directly on raw symbols like words or IDs, so they map them into continuous vectors. During training, the model learns embeddings that capture useful structure, so tokens or items used in similar contexts often end up with related representations. The term is used both for the input lookup table inside a model and for standalone vector outputs used in search and retrieval systems.",
    whyItMatters: "Embeddings are how models turn symbolic data into something learnable. They are also the backbone of semantic search, retrieval, clustering, and many practical AI applications outside direct text generation.",
    relatedTerms: ["Token", "LLM (Large Language Model)"],
  },
  {
    id: "llm-large-language-model",
    term: "LLM (Large Language Model)",
    category: "ai",
    tags: ["llms", "language", "foundation-models"],
    oneLiner: "An LLM is a large neural model trained to predict and generate sequences of tokens.",
    body: "Most LLMs are transformers trained on massive text and code corpora with a next-token prediction objective. By learning statistical structure at large scale, they acquire capabilities like writing, summarization, translation, coding assistance, and basic reasoning patterns. The 'large' part refers not just to parameter count, but also to training data, compute, and the broad range of behaviors that emerge from scale.",
    whyItMatters: "LLMs are the main engine behind today's AI products. Engineers working with them need a clear picture of what they are actually optimized to do and where their limits come from.",
    relatedTerms: ["Foundation Model", "GPT (Generative Pre-trained Transformer)"],
  },
  {
    id: "foundation-model",
    term: "Foundation Model",
    category: "ai",
    tags: ["llms", "pretraining", "platforms"],
    oneLiner: "A foundation model is a broadly trained base system meant to support many downstream tasks.",
    body: "The idea is to pretrain a large model once on diverse data, then adapt it with prompting, fine-tuning, or task-specific heads. Unlike a narrow model built for one benchmark, a foundation model is intended as a reusable substrate that generalizes across many applications. Language models, image generators, and multimodal systems can all fit this category if they serve as broad starting points.",
    whyItMatters: "Foundation models change how teams build AI systems: instead of training from scratch, they start from a powerful general model and specialize it. That shift affects product design, infrastructure, cost, and evaluation strategy.",
    relatedTerms: ["LLM (Large Language Model)", "Base Model"],
  },
  {
    id: "base-model",
    term: "Base Model",
    category: "ai",
    tags: ["llms", "pretraining", "model-lifecycle"],
    oneLiner: "A base model is the pretrained model before instruction tuning, RLHF, or task-specific adaptation.",
    body: "A base model is typically trained on raw data with a general objective such as next-token prediction. It has broad knowledge and pattern completion ability, but it may not follow instructions cleanly or align well with product-facing behavior. Later training stages can turn that base into a chat model, coding assistant, classifier, or domain-specialized system.",
    whyItMatters: "The distinction between base and adapted models explains why two systems with the same architecture can behave very differently. It also matters when deciding what kind of customization a project actually needs.",
    relatedTerms: ["Foundation Model", "GPT (Generative Pre-trained Transformer)"],
  },
  {
    id: "gpt-generative-pre-trained-transformer",
    term: "GPT (Generative Pre-trained Transformer)",
    category: "ai",
    tags: ["llms", "transformers", "language"],
    oneLiner: "GPT names a decoder-style transformer trained in advance to generate text token by token.",
    body: "The name breaks down into three pieces: generative because it produces new sequences, pre-trained because it is learned on large corpora before specific use, and transformer because the underlying architecture is attention-based. GPT-style models are typically decoder-only and autoregressive, meaning each new token is predicted from prior context. That simple training setup turned out to scale remarkably well.",
    whyItMatters: "GPT-style models shaped the modern LLM landscape and many of the assumptions engineers make about prompting and inference. If you understand GPT, you understand the default template behind a large share of language AI systems.",
    relatedTerms: ["Transformer", "Autoregressive Model"],
  },
  {
    id: "autoregressive-model",
    term: "Autoregressive Model",
    category: "ai",
    tags: ["llms", "generation", "probability"],
    oneLiner: "An autoregressive model generates a sequence by predicting the next piece from the pieces already seen.",
    body: "For language models, that usually means estimating the probability of the next token conditioned on all previous tokens in the context. Generation then proceeds one step at a time: sample or choose a token, append it, and repeat. This setup is simple, general, and powerful, but it also makes inference inherently sequential because later outputs depend on earlier ones.",
    whyItMatters: "Autoregression explains both the flexibility and latency profile of many language models. It is the reason token generation feels incremental and why caching prior computation matters so much in serving.",
    relatedTerms: ["GPT (Generative Pre-trained Transformer)", "Token"],
  },
  {
    id: "token",
    term: "Token",
    category: "ai",
    tags: ["llms", "language", "representation"],
    oneLiner: "A token is the chunk of text a language model actually reads and predicts.",
    body: "Tokens are not always words; they can be full words, subwords, punctuation, whitespace patterns, or pieces of code depending on the tokenizer. The model sees a sequence of token IDs, not raw characters, and each token gets mapped to an embedding before further processing. Token boundaries shape cost, context length, and how efficiently the model represents different languages or domains.",
    whyItMatters: "Engineers pay for tokens, reason about limits in tokens, and debug outputs at the token level. If you misunderstand tokenization, you will misread model behavior, latency, and cost.",
    relatedTerms: ["Tokenizer", "Context Window"],
  },
  {
    id: "tokenizer",
    term: "Tokenizer",
    category: "ai",
    tags: ["llms", "preprocessing", "language"],
    oneLiner: "A tokenizer converts raw text into the token sequence the model can actually process.",
    body: "The tokenizer defines a vocabulary and a set of rules for splitting input text into token IDs, along with the reverse mapping for decoding model outputs. This step happens before the neural network sees anything, which means many practical quirks come from tokenizer design rather than the model weights themselves. Different tokenizers trade off vocabulary size, compression efficiency, multilingual coverage, and behavior on rare strings.",
    whyItMatters: "Tokenizer design affects context usage, pricing, latency, and even qualitative behavior on code or non-English text. It is easy to ignore, but it is one of the most concrete levers in any LLM system.",
    relatedTerms: ["Token", "BPE (Byte Pair Encoding)"],
  },
  {
    id: "bpe-byte-pair-encoding",
    term: "BPE (Byte Pair Encoding)",
    category: "ai",
    tags: ["llms", "tokenization", "language"],
    oneLiner: "BPE builds subword tokens by repeatedly merging frequently adjacent symbol pairs.",
    body: "The process starts from small units such as bytes or characters and learns which neighboring pairs appear together often enough to merge into a new token. Over many merges, common words and fragments become single units while rare words remain split into smaller pieces. This gives a practical compromise between character-level flexibility and word-level efficiency.",
    whyItMatters: "BPE and related schemes are why language models can handle open-ended text without requiring a vocabulary entry for every possible word. It is a central piece of how modern tokenizers balance compression and coverage.",
    relatedTerms: ["Tokenizer", "Token"],
  },
  {
    id: "context-window",
    term: "Context Window",
    category: "ai",
    tags: ["llms", "inference", "limits"],
    oneLiner: "The context window is the maximum amount of token history the model can attend to in one pass.",
    body: "It includes the prompt, system instructions, retrieved documents, tool outputs, and the model's own generated tokens that remain in scope. Once the window fills up, older information must be dropped, summarized, or otherwise compressed because the model cannot attend to more than its limit. Larger context windows improve some workflows, but they also raise compute and memory costs.",
    whyItMatters: "Context length is one of the first hard constraints engineers hit when building real LLM applications. It shapes prompt design, retrieval strategy, latency, and whether a workflow can stay coherent over long interactions.",
    relatedTerms: ["Token", "Paged Attention"],
  },
  {
    id: "perplexity",
    term: "Perplexity",
    category: "ai",
    tags: ["llms", "evaluation", "metrics"],
    oneLiner: "Perplexity measures how surprised a language model is by the actual next tokens in a dataset.",
    body: "Formally, perplexity is derived from the model's average log loss on held-out text: lower perplexity means the model assigned higher probability to the observed tokens. It is a useful intrinsic metric for next-token prediction quality, but it is not a full proxy for helpfulness, factuality, or task success. Two models can have similar perplexity and still behave very differently in interactive use.",
    whyItMatters: "Perplexity is one of the cleanest ways to compare raw language modeling ability during training and evaluation. Engineers should use it, but not mistake it for a complete measure of product quality.",
    relatedTerms: ["Autoregressive Model", "Scaling Laws"],
  },
  {
    id: "scaling-laws",
    term: "Scaling Laws",
    category: "ai",
    tags: ["llms", "training", "research"],
    oneLiner: "Scaling laws describe the predictable way model performance changes as you increase size, data, and compute.",
    body: "Empirically, many model families improve smoothly as parameter count, dataset size, and training compute grow, often following power-law-like trends over broad ranges. These relationships help researchers estimate how much gain to expect from a larger run and how to balance model size against token budget. They do not guarantee every property improves equally, but they explain why simply making models bigger kept working for so long.",
    whyItMatters: "Scaling laws are a planning tool as much as a research result. They shape training budgets, architecture bets, and expectations about what scale alone can and cannot buy.",
    relatedTerms: ["LLM (Large Language Model)", "Perplexity"],
  },
];
