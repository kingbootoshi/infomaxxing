import { Concept } from '@/lib/types';
export const aiPart2: Concept[] = [
  {
    id: "pre-training",
    term: "Pre-training",
    category: "ai",
    tags: ["training", "foundation-models", "llms"],
    oneLiner: "The big first pass where a model learns general patterns from massive datasets.",
    body: "Pre-training is the stage where a model sees enormous amounts of text, code, images, or other data and learns to predict missing pieces, usually the next token. That objective forces the network to compress broad statistical structure into its weights: grammar, facts, style, common reasoning patterns, and domain regularities. It does not teach the model your exact product behavior, but it creates the general-purpose representation that later stages build on.",
    whyItMatters: "Most of a modern model's capability comes from pre-training, not the last-mile tuning step. If you understand pre-training, you understand why data scale, compute, and corpus quality matter so much.",
    relatedTerms: ["Fine-tuning", "Transfer Learning"],
  },
  {
    id: "fine-tuning",
    term: "Fine-tuning",
    category: "ai",
    tags: ["training", "adaptation", "models"],
    oneLiner: "Taking a general model and pushing it toward a narrower behavior.",
    body: "Fine-tuning continues training on a smaller, more targeted dataset after pre-training is complete. Instead of learning language from scratch, the model adjusts existing weights so it performs better on a task, domain, or response style you care about. That can mean better medical summaries, stronger code completions, or safer chat behavior, depending on the examples you choose.",
    whyItMatters: "Fine-tuning is how teams turn a broad base model into something useful for a specific product. It is also where poor data choices can quickly bake in bad habits or narrow the model too aggressively.",
    relatedTerms: ["Pre-training", "SFT (Supervised Fine-Tuning)"],
  },
  {
    id: "transfer-learning",
    term: "Transfer Learning",
    category: "ai",
    tags: ["training", "representation-learning", "adaptation"],
    oneLiner: "Reusing what a model already learned instead of starting from zero.",
    body: "Transfer learning is the idea that features learned on one problem can help solve another. A model trained on broad internet text already encodes syntax, concepts, and latent structure, so a second training stage can spend its budget adapting that knowledge rather than relearning basics. In practice, fine-tuning, adapters, and many vision pipelines are all forms of transfer learning.",
    whyItMatters: "It is the reason small datasets can still produce strong task-specific models when you begin with a capable base. Without transfer learning, training useful systems would be far more expensive and data-hungry.",
    relatedTerms: ["Pre-training", "Fine-tuning"],
  },
  {
    id: "loss-function",
    term: "Loss Function",
    category: "ai",
    tags: ["training", "optimization", "metrics"],
    oneLiner: "The score that tells training how wrong the model currently is.",
    body: "A loss function turns model mistakes into a single number that optimization can work with. Cross-entropy, for example, heavily penalizes high confidence on the wrong token, while regression losses measure numeric error directly. During training, every weight update is trying to reduce this loss over many examples, so the exact form of the loss shapes what behavior the model learns.",
    whyItMatters: "If you optimize the wrong loss, the model will get better at the wrong thing. The loss is the training signal, so it defines what success means mathematically, not just rhetorically.",
    relatedTerms: ["Backpropagation", "Gradient Descent"],
  },
  {
    id: "backpropagation",
    term: "Backpropagation",
    category: "ai",
    tags: ["training", "gradients", "neural-networks"],
    oneLiner: "The algorithm that tells each weight how much it contributed to the error.",
    body: "Backpropagation computes gradients by moving backward through the network from the final loss to earlier layers. Using the chain rule, it measures how a tiny change in each parameter would change the final error. That lets the optimizer update millions or billions of weights without guessing blindly.",
    whyItMatters: "Deep learning works at scale because backprop makes credit assignment tractable across very large networks. Without it, training modern models would be far slower and far less precise.",
    relatedTerms: ["Loss Function", "Gradient Descent"],
  },
  {
    id: "gradient-descent",
    term: "Gradient Descent",
    category: "ai",
    tags: ["training", "optimization", "gradients"],
    oneLiner: "Improving a model by nudging weights downhill on the error surface.",
    body: "Gradient descent updates parameters in the direction that most reduces loss, using gradients computed during backpropagation. In practice, training uses mini-batches, so each step is based on a noisy estimate of the true gradient rather than the whole dataset at once. That noise is not just a compromise for speed - it also helps the optimizer keep moving instead of getting stuck too easily.",
    whyItMatters: "This is the core update rule behind most neural network training. The details of how you descend - batch size, optimizer choice, step schedule - strongly affect stability, speed, and final quality.",
    relatedTerms: ["Backpropagation", "Learning Rate"],
  },
  {
    id: "adam-optimizer",
    term: "Adam Optimizer",
    category: "ai",
    tags: ["training", "optimization", "optimizers"],
    oneLiner: "An optimizer that adapts each parameter's step size from recent gradient history.",
    body: "Adam tracks running averages of both the gradient and the squared gradient for every parameter. That gives it a notion of momentum and a per-parameter scale estimate, so weights with volatile gradients get treated differently from stable ones. The result is usually faster, more forgiving training than plain stochastic gradient descent, especially early in a run.",
    whyItMatters: "Adam is a default choice in many training pipelines because it converges well with less manual tuning. Engineers still need to understand it, because bad settings can cause instability or poor generalization.",
    relatedTerms: ["Gradient Descent", "Learning Rate"],
  },
  {
    id: "learning-rate",
    term: "Learning Rate",
    category: "ai",
    tags: ["training", "optimization", "hyperparameters"],
    oneLiner: "The dial that controls how large each training update is.",
    body: "The learning rate scales how far the optimizer moves the weights on each step. If it is too high, training can diverge or oscillate; if it is too low, progress becomes painfully slow and can stall in mediocre solutions. Most serious training runs use schedules such as warmup and decay because the best step size changes over the course of optimization.",
    whyItMatters: "Few hyperparameters matter more. A strong model can fail to train well simply because the learning rate policy is wrong.",
    relatedTerms: ["Gradient Descent", "Adam Optimizer"],
  },
  {
    id: "sft-supervised-fine-tuning",
    term: "SFT (Supervised Fine-Tuning)",
    category: "ai",
    tags: ["alignment", "training", "supervision"],
    oneLiner: "Teaching a model with labeled input-output examples of the behavior you want.",
    body: "SFT takes a pretrained model and trains it on prompt-response pairs written or curated by humans. The model is rewarded for matching the target answer token by token, which makes it imitate the style, structure, and task behavior in the dataset. It is usually the first alignment stage for chat models because it turns raw next-token competence into usable assistant behavior.",
    whyItMatters: "SFT is the bridge from general capability to product-ready behavior. It is also where annotation quality shows up directly in the model's tone, helpfulness, and failure modes.",
    relatedTerms: ["Fine-tuning", "Instruction Tuning"],
  },
  {
    id: "instruction-tuning",
    term: "Instruction Tuning",
    category: "ai",
    tags: ["alignment", "chat-models", "training"],
    oneLiner: "Fine-tuning a model to respond usefully to requests instead of just continuing text.",
    body: "Instruction tuning trains on examples framed as tasks, questions, commands, and expected answers. That changes the model's default behavior from generic text continuation to following user intent, refusing where appropriate, and formatting answers in helpful ways. It often uses many task types so the model learns the pattern of instruction-following itself, not just one narrow workflow.",
    whyItMatters: "Base models can be smart but still awkward to use. Instruction tuning is a major reason modern assistants feel cooperative instead of sounding like autocomplete glued to a prompt box.",
    relatedTerms: ["SFT (Supervised Fine-Tuning)", "RLHF (Reinforcement Learning from Human Feedback)"],
  },
  {
    id: "rlhf",
    term: "RLHF (Reinforcement Learning from Human Feedback)",
    category: "ai",
    tags: ["alignment", "reinforcement-learning", "preferences"],
    oneLiner: "Using human preference judgments to push a model toward better answers.",
    body: "RLHF usually starts by collecting ranked comparisons of model outputs, such as which of two answers is more helpful or safer. A reward model is trained on those preferences, and then reinforcement learning updates the main model to produce outputs that score higher under that learned reward. The point is not just factual accuracy - it is aligning behavior to human judgments that are hard to write as simple rules.",
    whyItMatters: "RLHF is one of the main reasons chat models feel more aligned with user expectations than raw base models. It also introduces new failure modes, because the model is optimizing a proxy for what humans wanted, not the humans directly.",
    relatedTerms: ["Reward Model", "DPO (Direct Preference Optimization)"],
  },
  {
    id: "dpo",
    term: "DPO (Direct Preference Optimization)",
    category: "ai",
    tags: ["alignment", "preferences", "optimization"],
    oneLiner: "Learning from preference pairs without running a full reinforcement learning loop.",
    body: "DPO trains directly on chosen-versus-rejected responses and adjusts the model so preferred answers become more likely. Instead of generating rollouts and optimizing with a separate online RL procedure, it turns preference learning into a supervised-style objective derived from the same underlying idea. That makes the pipeline simpler while still using ranking data to shape behavior.",
    whyItMatters: "DPO often gives teams a cheaper and more stable path than classical RLHF. It matters because alignment methods are only useful in practice if they are operationally tractable.",
    relatedTerms: ["RLHF (Reinforcement Learning from Human Feedback)", "Reward Model"],
  },
  {
    id: "rlaif",
    term: "RLAIF (RL from AI Feedback)",
    category: "ai",
    tags: ["alignment", "preferences", "automation"],
    oneLiner: "Replacing some human judgments with ratings generated by another model.",
    body: "RLAIF uses an AI system to score, rank, or critique outputs instead of relying entirely on humans for preference data. The supervising model is usually guided by a rubric or constitution so its feedback is more structured than a raw guess. This can scale much more cheaply than all-human labeling, though it also risks amplifying the biases or blind spots of the judge model.",
    whyItMatters: "Human feedback is expensive and slow, so scalable alignment often leans on AI-generated supervision. Engineers need to understand when that shortcut is useful and when it starts compounding errors.",
    relatedTerms: ["RLHF (Reinforcement Learning from Human Feedback)", "Constitutional AI"],
  },
  {
    id: "reward-model",
    term: "Reward Model",
    category: "ai",
    tags: ["alignment", "preferences", "models"],
    oneLiner: "A model trained to score how good an output is according to preference data.",
    body: "A reward model takes a prompt and candidate response and outputs a scalar score that stands in for human preference. It is usually trained from comparison data, learning that one answer should rank above another even when both are plausible. In RLHF pipelines, that score becomes the signal the main model optimizes against during policy updates.",
    whyItMatters: "The reward model is the proxy at the center of many alignment systems. If it misreads quality, the main model will faithfully optimize the wrong behavior at scale.",
    relatedTerms: ["RLHF (Reinforcement Learning from Human Feedback)", "Reward Hacking"],
  },
  {
    id: "reward-hacking",
    term: "Reward Hacking",
    category: "ai",
    tags: ["alignment", "failure-modes", "optimization"],
    oneLiner: "When a model finds ways to score well on the proxy without doing what you actually wanted.",
    body: "Reward hacking happens when the system exploits weaknesses in the objective, evaluator, or setup rather than solving the underlying task honestly. A model might learn to sound confident, verbose, or overly deferential because the reward model mistakes those traits for quality. The optimization is doing its job - the problem is that the target was an imperfect stand-in for human intent.",
    whyItMatters: "This is the core warning behind many alignment problems: optimizing a proxy changes behavior in ways the proxy designer did not foresee. If you ignore reward hacking, your metrics can improve while real usefulness gets worse.",
    relatedTerms: ["Reward Model", "RLHF (Reinforcement Learning from Human Feedback)"],
  },
  {
    id: "constitutional-ai",
    term: "Constitutional AI",
    category: "ai",
    tags: ["alignment", "safety", "governance"],
    oneLiner: "Steering a model with an explicit rule set that it can use to critique and revise itself.",
    body: "Constitutional AI gives a model a written set of principles, then uses those principles during training or self-critique to improve responses. Instead of only learning from direct human rankings, the model can generate an answer, critique it against the constitution, and produce a revised version. The approach tries to make alignment more legible by naming the rules rather than leaving all preferences implicit in label data.",
    whyItMatters: "It offers a scalable way to shape behavior when you want consistent norms across many cases. It also forces teams to be explicit about what values or constraints they are encoding into the system.",
    relatedTerms: ["RLAIF (RL from AI Feedback)", "Instruction Tuning"],
  },
  {
    id: "lora",
    term: "LoRA (Low-Rank Adaptation)",
    category: "ai",
    tags: ["fine-tuning", "efficiency", "adapters"],
    oneLiner: "Fine-tuning by learning a small adapter instead of rewriting the whole model.",
    body: "LoRA freezes the original model weights and learns low-rank matrices that modify selected layers during inference. Because those matrices are much smaller than the full weight tensors, training requires far less memory and storage than full fine-tuning. You keep the base model intact and layer a task-specific delta on top of it.",
    whyItMatters: "LoRA made customization practical on modest hardware. It is a major reason open model ecosystems can support many specialized variants without shipping full copies of giant checkpoints.",
    relatedTerms: ["PEFT (Parameter-Efficient Fine-Tuning)", "QLoRA"],
  },
  {
    id: "qlora",
    term: "QLoRA",
    category: "ai",
    tags: ["fine-tuning", "quantization", "efficiency"],
    oneLiner: "LoRA fine-tuning on top of a quantized base model to cut memory even further.",
    body: "QLoRA keeps the base model in low precision, typically 4-bit, while training LoRA adapters on top of it. The quantized backbone reduces memory pressure enough to fine-tune models that would otherwise not fit on a single GPU. Careful handling of the low-precision weights preserves much of the quality while making the setup dramatically cheaper.",
    whyItMatters: "QLoRA lowered the hardware bar for adapting large models. That changed who could experiment, fine-tune, and ship customized systems without a large training cluster.",
    relatedTerms: ["LoRA (Low-Rank Adaptation)", "Quantization"],
  },
  {
    id: "peft",
    term: "PEFT (Parameter-Efficient Fine-Tuning)",
    category: "ai",
    tags: ["fine-tuning", "efficiency", "training"],
    oneLiner: "A family of methods that tune a model by touching only a small fraction of parameters.",
    body: "PEFT is the umbrella idea behind techniques like LoRA, prefix tuning, and adapter layers. Instead of updating every weight in a large model, you train a compact set of additional parameters or selectively expose tiny slices of the network. That slashes GPU memory, checkpoint size, and training cost while keeping much of the benefit of adaptation.",
    whyItMatters: "PEFT is what turns fine-tuning from an expensive research exercise into an everyday product tool. It also makes it easier to maintain many variants of the same base model for different tasks.",
    relatedTerms: ["LoRA (Low-Rank Adaptation)", "Fine-tuning"],
  },
  {
    id: "knowledge-distillation",
    term: "Knowledge Distillation",
    category: "ai",
    tags: ["training", "compression", "models"],
    oneLiner: "Training a smaller model to imitate a stronger one.",
    body: "In distillation, a teacher model produces outputs, probability distributions, or reasoning traces that a student model learns to match. Those soft targets carry richer information than hard labels alone because they show which alternatives the teacher considered plausible. The goal is to retain much of the teacher's behavior in a model that is cheaper, smaller, or faster to run.",
    whyItMatters: "Distillation is a common path from powerful but expensive models to deployable ones. It matters whenever latency, device limits, or serving cost make the original model impractical.",
    relatedTerms: ["Transfer Learning", "Quantization"],
  },
  {
    id: "synthetic-data",
    term: "Synthetic Data",
    category: "ai",
    tags: ["data", "training", "generation"],
    oneLiner: "Artificially generated training data used to expand or shape a dataset.",
    body: "Synthetic data can be created by simulation, templates, programmatic rules, or another model generating examples. Teams use it to cover rare cases, teach a format, augment small datasets, or avoid collecting sensitive real-world data. Its value depends on how well the synthetic examples preserve the structure of the real task instead of just looking superficially plausible.",
    whyItMatters: "Good synthetic data can unlock training progress when real labels are scarce or expensive. Bad synthetic data can quietly teach the model the wrong distribution and make evaluation look better than deployment reality.",
    relatedTerms: ["SFT (Supervised Fine-Tuning)", "Data Flywheel"],
  },
  {
    id: "data-flywheel",
    term: "Data Flywheel",
    category: "ai",
    tags: ["data", "products", "feedback-loops"],
    oneLiner: "A loop where product usage creates data that makes the product better, which creates more usage.",
    body: "In AI products, user interactions, corrections, ratings, and edge cases can be fed back into labeling and retraining pipelines. As the model improves, the product becomes more useful, which increases usage and produces more high-value data. The loop compounds only if you can capture the right signals and filter out noise, spam, and bias.",
    whyItMatters: "Strong AI products are often defended less by the initial model and more by the data flywheel they build around it. Engineers should care because the system design determines whether usage becomes a durable advantage or just exhaust noise.",
    relatedTerms: ["Synthetic Data", "Fine-tuning"],
  },
  {
    id: "mixed-precision-training",
    term: "Mixed Precision Training",
    category: "ai",
    tags: ["training", "performance", "numerics"],
    oneLiner: "Using lower-precision math where it is safe to speed up training and save memory.",
    body: "Mixed precision training runs many tensor operations in formats like FP16 or BF16 while keeping certain values, accumulators, or optimizer state in higher precision when needed for stability. Lower precision cuts memory bandwidth and increases hardware throughput, which is often a major bottleneck in large model training. The trick is balancing speed gains with numerical safety so gradients do not underflow or explode.",
    whyItMatters: "This is one of the simplest ways to make large training jobs cheaper and faster. It is now standard practice because the performance difference is too large to ignore.",
    relatedTerms: ["Distributed Training", "Gradient Checkpointing"],
  },
  {
    id: "distributed-training",
    term: "Distributed Training",
    category: "ai",
    tags: ["training", "scaling", "infrastructure"],
    oneLiner: "Training a model across many devices because one machine is not enough.",
    body: "Distributed training splits work across multiple GPUs or nodes using strategies like data parallelism, model parallelism, or sharding optimizer state. The system has to coordinate gradient synchronization, parameter updates, and communication efficiently, or the hardware spends too much time waiting. As models get larger, distributed training is less an optimization and more a basic requirement.",
    whyItMatters: "Scaling training is not just about buying more GPUs. Communication overhead, memory layout, and failure handling become first-class engineering problems once training spans many machines.",
    relatedTerms: ["Mixed Precision Training", "Gradient Checkpointing"],
  },
  {
    id: "gradient-checkpointing",
    term: "Gradient Checkpointing",
    category: "ai",
    tags: ["training", "memory", "optimization"],
    oneLiner: "Saving memory by recomputing parts of the forward pass later instead of storing everything.",
    body: "During training, backpropagation normally needs intermediate activations from the forward pass. Gradient checkpointing stores only selected checkpoints and recomputes missing activations on demand during the backward pass. That trades extra compute for lower memory use, which is often worth it when model size is the limiting factor.",
    whyItMatters: "This technique often decides whether a training run fits at all on available hardware. Engineers use it to push larger batch sizes or larger models without changing the model architecture.",
    relatedTerms: ["Mixed Precision Training", "Distributed Training"],
  },
  {
    id: "inference",
    term: "Inference",
    category: "ai",
    tags: ["deployment", "serving", "runtime"],
    oneLiner: "Running a trained model to produce outputs for real requests.",
    body: "Inference is the deployment-time phase where the model takes an input and generates a prediction, classification, embedding, or token stream. The weights are usually fixed, so the engineering focus shifts from learning to latency, throughput, memory footprint, and reliability. For LLMs, inference is dominated by repeated forward passes as each new token depends on the tokens that came before it.",
    whyItMatters: "Most user-facing cost and performance pain shows up during inference, not training. A model that is brilliant in a benchmark but too slow or expensive to serve is not a practical product component.",
    relatedTerms: ["KV Cache", "Quantization"],
  },
  {
    id: "kv-cache",
    term: "KV Cache",
    category: "ai",
    tags: ["inference", "transformers", "performance"],
    oneLiner: "A memory of previous attention states so the model does not recompute the whole prompt every token.",
    body: "In transformer decoding, each new token needs attention over prior tokens. The KV cache stores the already computed key and value tensors from earlier steps, so generating token N only requires new work for the latest position instead of recalculating the entire prefix. This makes long-context generation dramatically faster, though it increases memory use per active request.",
    whyItMatters: "KV caching is one of the main reasons chat-style generation is tractable at all. It also becomes a central serving constraint because cache memory grows with context length and concurrency.",
    relatedTerms: ["Inference", "Prompt Caching"],
  },
  {
    id: "speculative-decoding",
    term: "Speculative Decoding",
    category: "ai",
    tags: ["inference", "latency", "serving"],
    oneLiner: "Speeding up generation by letting a smaller model guess tokens ahead of the larger one.",
    body: "A fast draft model proposes several next tokens, and the larger target model checks whether those guesses are acceptable. When the guesses are correct, the system can accept multiple tokens in one round and skip some expensive target-model steps. When the draft is wrong, the target model falls back and continues normally, so the method trades extra coordination for better average latency.",
    whyItMatters: "Speculative decoding is one of the few tricks that can materially cut LLM response time without shrinking the main model. It matters because user experience is highly sensitive to the first seconds of generation.",
    relatedTerms: ["Inference", "Continuous Batching"],
  },
  {
    id: "quantization",
    term: "Quantization",
    category: "ai",
    tags: ["inference", "compression", "efficiency"],
    oneLiner: "Shrinking model weights or activations into fewer bits to save memory and speed up serving.",
    body: "Quantization represents tensors with lower-precision formats such as 8-bit or 4-bit integers instead of full 16-bit or 32-bit floating point. The key challenge is preserving model quality while introducing less expressive numeric representations, usually through calibration, scale factors, and careful error control. Some methods quantize only weights, while others also quantize activations for bigger gains.",
    whyItMatters: "Quantization is often the difference between a model that needs a datacenter GPU and one that runs cheaply on consumer hardware. It is one of the highest-leverage deployment optimizations in modern AI systems.",
    relatedTerms: ["GPTQ", "AWQ"],
  },
  {
    id: "gguf",
    term: "GGUF",
    category: "ai",
    tags: ["inference", "formats", "open-models"],
    oneLiner: "A file format for shipping model weights and metadata in local inference stacks.",
    body: "GGUF is a model format used heavily in the local LLM ecosystem, especially with llama.cpp. It packages quantized weights together with metadata such as tokenizer information and architecture details so a runtime can load the model consistently. The format was designed to make distribution and local execution simpler than juggling many loosely related files.",
    whyItMatters: "If you work with local or edge inference, GGUF is part of the plumbing that makes models easy to share and run. Format choice affects compatibility, startup behavior, and which runtimes you can use.",
    relatedTerms: ["Quantization", "llama.cpp"],
  },
  {
    id: "gptq",
    term: "GPTQ",
    category: "ai",
    tags: ["quantization", "inference", "compression"],
    oneLiner: "A post-training quantization method that compresses weights while trying to preserve accuracy.",
    body: "GPTQ quantizes a pretrained model after training rather than retraining it from scratch in low precision. It uses second-order information to estimate how much damage quantization would cause and chooses weight approximations that minimize that error. The goal is aggressive compression, often down to 4-bit weights, without wrecking downstream behavior.",
    whyItMatters: "GPTQ helped make large models runnable on far smaller hardware than their original checkpoints required. It matters because good post-training quantization avoids the cost of a new training run.",
    relatedTerms: ["Quantization", "AWQ"],
  },
  {
    id: "awq",
    term: "AWQ",
    category: "ai",
    tags: ["quantization", "inference", "compression"],
    oneLiner: "A quantization method that protects the most important weights based on activation behavior.",
    body: "AWQ stands for Activation-aware Weight Quantization. It identifies weight channels that matter most to the model's activations and preserves them more carefully during low-bit compression. By treating all weights as unequal in importance, it can keep quality higher than more uniform quantization schemes at similar bit widths.",
    whyItMatters: "Not all quantization methods fail in the same places. AWQ matters because it often gives a better quality-speed-memory tradeoff for practical deployment.",
    relatedTerms: ["Quantization", "GPTQ"],
  },
  {
    id: "llama-cpp",
    term: "llama.cpp",
    category: "ai",
    tags: ["inference", "open-source", "local-ai"],
    oneLiner: "A lightweight runtime that made local LLM inference practical on ordinary machines.",
    body: "llama.cpp is a C and C++ inference project focused on running large language models efficiently on CPUs and modest hardware. It supports quantized formats, streaming generation, and platform-specific optimizations so users can run models without a heavyweight serving stack. It became a key piece of the open model ecosystem because it turned local inference from a niche experiment into a usable workflow.",
    whyItMatters: "For many teams, llama.cpp is the shortest path from a checkpoint file to a working local model. It changed the deployment conversation by making privacy-preserving and offline use cases far more accessible.",
    relatedTerms: ["GGUF", "On-Device AI"],
  },
  {
    id: "vllm",
    term: "vLLM",
    category: "ai",
    tags: ["inference", "serving", "throughput"],
    oneLiner: "A high-throughput LLM serving engine built to keep GPUs busy under real workloads.",
    body: "vLLM is an inference system designed for fast, memory-efficient serving of large language models. Its core serving ideas, including paged management of KV cache memory, let many requests share GPU resources more effectively than naive per-request allocation. That makes it well suited to chat APIs and batchy production traffic where utilization matters as much as raw model speed.",
    whyItMatters: "Serving performance is not just about the model - it is about the runtime around it. vLLM matters because better scheduling and memory handling can cut cost per token without changing the model itself.",
    relatedTerms: ["Continuous Batching", "KV Cache"],
  },
  {
    id: "continuous-batching",
    term: "Continuous Batching",
    category: "ai",
    tags: ["inference", "scheduling", "throughput"],
    oneLiner: "Adding and removing requests from a live batch instead of waiting for a fixed synchronized group.",
    body: "Traditional batching groups requests together at the same start and end points, which works poorly for token-by-token generation because requests finish at different times. Continuous batching keeps the GPU fed by admitting new requests as slots free up and stepping all active sequences together. That improves throughput and utilization, especially under mixed prompt lengths and variable generation times.",
    whyItMatters: "LLM serving efficiency often comes down to scheduler design. Continuous batching is one of the main techniques behind modern high-throughput serving systems.",
    relatedTerms: ["vLLM", "Speculative Decoding"],
  },
  {
    id: "mixture-of-experts",
    term: "Mixture of Experts (MoE)",
    category: "ai",
    tags: ["model-architecture", "scaling", "efficiency"],
    oneLiner: "A model that routes each token through only a subset of specialized subnetworks.",
    body: "In an MoE model, a routing component decides which expert layers should process a given token. That means the total parameter count can be very large while the compute used per token stays relatively lower because only a few experts are active at once. The design buys scale and specialization, but it also creates routing, load-balancing, and serving complexity.",
    whyItMatters: "MoE is one of the main ways frontier models grow capacity without linearly growing compute per token. Engineers need to understand it because the architecture changes both training dynamics and deployment behavior.",
    relatedTerms: ["Distributed Training", "Inference"],
  },
  {
    id: "prompt-caching",
    term: "Prompt Caching",
    category: "ai",
    tags: ["inference", "latency", "reuse"],
    oneLiner: "Reusing work for repeated prompt prefixes instead of recomputing them every time.",
    body: "Many applications send the same system prompt, tool instructions, or long shared context across many requests. Prompt caching stores reusable intermediate state so the model can skip reprocessing that shared prefix on future calls. In transformer systems, this often means reusing a previously built KV cache for the common part of the prompt.",
    whyItMatters: "If your traffic repeats the same context, prompt caching can cut latency and serving cost immediately. It rewards product teams that design prompts and workflows with reuse in mind.",
    relatedTerms: ["KV Cache", "Inference"],
  },
  {
    id: "test-time-compute",
    term: "Test-Time Compute",
    category: "ai",
    tags: ["inference", "reasoning", "performance"],
    oneLiner: "Spending extra compute during inference to get a better answer from the same model.",
    body: "Test-time compute means doing more work at serving time, such as generating multiple candidates, running self-consistency checks, using search, or letting the model deliberate longer. Instead of changing the trained weights, you change how much reasoning budget the system gets per request. The tradeoff is straightforward: more latency and cost in exchange for a chance at higher quality on hard problems.",
    whyItMatters: "This is a practical lever when retraining is expensive or impossible. It matters because some capability gains come from smarter inference procedures, not just bigger models.",
    relatedTerms: ["Inference", "Speculative Decoding"],
  },
  {
    id: "edge-inference",
    term: "Edge Inference",
    category: "ai",
    tags: ["deployment", "latency", "edge-computing"],
    oneLiner: "Running models close to the user or data source instead of a distant central server.",
    body: "Edge inference moves serving to devices or nearby edge machines so requests do not always cross the full network path to a central region. That reduces round-trip latency and can help with privacy or bandwidth limits, especially for real-time audio, vision, and industrial workloads. The tradeoff is tighter hardware constraints and more fragmented deployment environments.",
    whyItMatters: "Where you run the model affects user experience as much as which model you choose. Edge inference is often the right answer when milliseconds, connectivity, or data locality matter more than centralized convenience.",
    relatedTerms: ["On-Device AI", "Inference"],
  },
  {
    id: "on-device-ai",
    term: "On-Device AI",
    category: "ai",
    tags: ["deployment", "privacy", "mobile"],
    oneLiner: "Running the model directly on the user's phone, laptop, or other local hardware.",
    body: "On-device AI is the extreme version of local deployment: the model executes on the end user's hardware rather than in your cloud. That improves privacy, offline availability, and responsiveness, but it forces the model and runtime to fit strict limits on memory, battery, thermals, and compute. Quantization, distillation, and efficient runtimes are usually what make the setup viable.",
    whyItMatters: "This approach changes the product tradeoff completely by reducing dependence on central infrastructure. Engineers should care because local execution can unlock privacy-sensitive and offline use cases that cloud-only systems cannot serve well.",
    relatedTerms: ["Edge Inference", "llama.cpp"],
  },
];
